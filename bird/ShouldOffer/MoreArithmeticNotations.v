From Maniunfold.Offers Require Export
  Arithmetic.
From Maniunfold.ShouldHave Require Export
  ArithmeticNotations.

Notation "x '-' y" := (sub x y) : algebra_scope.
Notation "x '/' y" := (div x y) : algebra_scope.
